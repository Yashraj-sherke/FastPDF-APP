package com.fastpdf.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * FastPDF Color Palette
 * Derived from the reference UI — clean blue accent with neutral grays.
 */

// ━━━━━━━━━━━━━━━━━━━━━━━
// Primary
// ━━━━━━━━━━━━━━━━━━━━━━━
val Primary = Color(0xFF4A6CF7)
val PrimaryLight = Color(0xFFE8EDFF)
val PrimaryDark = Color(0xFF3451D1)
val OnPrimary = Color.White

// ━━━━━━━━━━━━━━━━━━━━━━━
// Surface & Background
// ━━━━━━━━━━━━━━━━━━━━━━━
val Surface = Color(0xFFFAFBFF)
val SurfaceVariant = Color(0xFFF3F4F8)
val OnSurface = Color(0xFF1A1C2E)
val OnSurfaceVariant = Color(0xFF6B7280)

val Background = Color.White
val OnBackground = Color(0xFF1A1C2E)

// ━━━━━━━━━━━━━━━━━━━━━━━
// Container
// ━━━━━━━━━━━━━━━━━━━━━━━
val PrimaryContainer = Color(0xFFE8EDFF)
val OnPrimaryContainer = Color(0xFF1A2B6D)
val SecondaryContainer = Color(0xFFF0F1F5)
val OnSecondaryContainer = Color(0xFF3B3F4A)

// ━━━━━━━━━━━━━━━━━━━━━━━
// Accents
// ━━━━━━━━━━━━━━━━━━━━━━━
val AccentOrange = Color(0xFFFF8C42)
val AccentGreen = Color(0xFF10B981)
val AccentRed = Color(0xFFEF4444)

// ━━━━━━━━━━━━━━━━━━━━━━━
// Outline & Divider
// ━━━━━━━━━━━━━━━━━━━━━━━
val Outline = Color(0xFFE5E7EB)
val OutlineVariant = Color(0xFFF0F1F5)

// ━━━━━━━━━━━━━━━━━━━━━━━
// Document Type Colors
// ━━━━━━━━━━━━━━━━━━━━━━━
val PdfRed = Color(0xFFE53935)
val WordBlue = Color(0xFF1565C0)
val ExcelGreen = Color(0xFF2E7D32)
val PptOrange = Color(0xFFE65100)
val ImagePurple = Color(0xFF7B1FA2)

// ━━━━━━━━━━━━━━━━━━━━━━━
// Gradient Colors
// ━━━━━━━━━━━━━━━━━━━━━━━
val GradientStart = Color(0xFF4A6CF7)
val GradientEnd = Color(0xFF6C8AFF)

// ━━━━━━━━━━━━━━━━━━━━━━━
// Navigation Bar
// ━━━━━━━━━━━━━━━━━━━━━━━
val NavBarSelected = Primary
val NavBarUnselected = Color(0xFF9CA3AF)
val NavBarIndicator = PrimaryLight

// ━━━━━━━━━━━━━━━━━━━━━━━
// Dark Theme Colors
// ━━━━━━━━━━━━━━━━━━━━━━━
val DarkBackground = Color(0xFF0F1118)
val DarkSurface = Color(0xFF171923)
val DarkSurfaceVariant = Color(0xFF1E2130)
val DarkOnSurface = Color(0xFFE4E6EB)
val DarkOnSurfaceVariant = Color(0xFF9CA3AF)
val DarkOutline = Color(0xFF2D3142)
val DarkOutlineVariant = Color(0xFF252838)
val DarkPrimaryContainer = Color(0xFF1E2A5E)
val DarkSecondaryContainer = Color(0xFF252838)

